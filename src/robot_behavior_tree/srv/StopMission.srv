# StopMission.srv - 停止任务服务

# 请求
string mission_id                  # 任务ID（空则停止当前所有任务）
bool force_stop                    # 强制停止（不等待当前动作完成）
---
# 响应
bool success                       # 是否成功停止
string message                     # 结果消息
float32 mission_duration           # 任务执行时间
